# Agent instructions

Read `CLAUDE.md` in this directory — it is the single project guide
(architecture, game rules, socket contract, reconnect behavior, commands).
Keep the two files from diverging: update `CLAUDE.md`, not this file.
